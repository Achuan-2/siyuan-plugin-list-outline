## v0.0.1 / 2026-09-15

- 列表大纲新增搜索功能，悬停展开大纲后可输入关键词实时过滤列表项、高亮匹配文字，支持按 Esc 快速清空搜索及收起后自动重置。
- 列表大纲新增「无子级时不显示列表大纲」设置项，开启后单层无子块（无嵌套子列表、引述块、代码块、附加段落等）的扁平列表不显示悬浮大纲，包含子块时正常显示。
- 列表大纲改为可视区域驱动：列表在可视区域内持续显示紧凑线条，鼠标移出列表块不隐藏，滚动出可视区后自动隐藏；支持可视区内多个列表同时显示对应大纲。
- 标题大纲可选包含列表项形成混合目录，面板及插件设置均可开关，默认关闭；沿用列表层级、引述过滤、定位及右键插入。

- 列表大纲新增鼠标、光标及滚动位置高亮，隐藏子层级时高亮最近父项。
- 修复原生子级标题的空 name 属性覆盖 content，导致三级等子标题误显示为空。

- 两种大纲支持右键向前／向后插入同级标题或列表项，并定位新块。
- 右键弹出插入菜单时，鼠标离开悬浮大纲不关闭或收起大纲。
- 列表大纲优先显示在正文右侧留白，减少遮挡。

- 新增悬浮标题大纲，使用思源原生标题树接口与折叠标题定位流程。
- 标题大纲和列表大纲可独立开关，默认开启，设置即时生效。
- 标题大纲支持文档切换、预览定位、内容刷新和滚动位置高亮。

- 大纲默认仅显示层级线条，悬停后展开文字与设置，移出后收起。
- 排除引述块中的列表项，避免引述内容混入列表大纲。

- 鼠标进入列表块时，在右上角显示悬浮大纲，点击条目定位列表项。
- 仅按列表项生成层级，支持有序、无序、任务列表及嵌套列表。
- 全局设置列表默认层级，默认 3 层；两种大纲均按面板宽度单行省略，取消每项显示字数限制。
- 每个列表可单独保存层级到块属性，并可恢复跟随默认。
- 支持内容更新、折叠列表内容读取、越界定位和卸载清理。
